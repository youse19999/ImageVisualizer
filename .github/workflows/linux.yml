# ++AIの書き込み [2026-06-01 12:30] [GitHub Actions: Linux - 全OS対応] [write_file] [deepseek-v4-flash]
# --AIの書き込み [2026-06-01 12:30]

name: CI - Linux

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Python ${{ matrix.python-version }} をセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: システム依存関係をインストール
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq python3-tk xvfb

      - name: Python依存関係をインストール
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: pytest でユニットテストを実行
        run: |
          python -m pytest tests/ -v --tb=short

      - name: スクリーンショットテスト（Linux xvfb上）
        run: |
          xvfb-run python test.py
